import { Typography } from "@mui/material";

export default function Footer() {
    return (
        <footer style={{backgroundColor: 'springgreen'}} >
            <Typography variant="h3" >Footer Component</Typography>
        </footer>
    )
}